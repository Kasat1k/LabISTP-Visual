@model ISTPLab.Subject

@{
    ViewData["Title"] = "Details";
}
<h1>Інформація про предмет @Model?.Name </h1>

<div>

    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<br />
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary" style="margin-right: 20px;" >Змінити</a> 
    <a asp-action="Index" class="btn btn-primary" style="margin-right: 20px;">Повернутись</a>
</div>
